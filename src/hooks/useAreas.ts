// useAreas is no longer needed — AreasPage uses useDiscoveredEntities directly.
// File kept as a barrel export for backwards compat if imported elsewhere.
export { useDiscoveredEntities as useAreas } from './useDiscoveredEntities'
